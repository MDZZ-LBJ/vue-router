import install f

class VueRouter {
    constructor(options) {
        this.mode = options.mode
        this.routes = options.routes
    }
    init(app) { // 路由的初始化

    }
}



export default VueRouter